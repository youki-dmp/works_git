# Local Markdown Viewer Extension

ローカルの `.md` ファイルをブラウザで開いた際に、GitHub風の美しいスタイルで表示し、目次機能やメディア再生機能を追加するChrome拡張機能です。

## 特徴

- **GitHub風プレミアムスタイル**: システム設定に連動するライト/ダークモード対応。
- **レスポンシブ目次 (ToC)**: 画面サイズに応じてフローティング表示やトグルメニューに自動切り替え。
- **YouTubeリンク自動埋め込み**: URLを iframe プレイヤーに自動変換。
- **スムーズな操作感**: 目次からのジャンプやサイドバーのスムーズな開閉。

## インストール方法

1.  このディレクトリをローカルに保存します。
2.  Chromeブラウザで `chrome://extensions/` を開きます。
3.  右上の「デベロッパー モード」をオンにします。
4.  「パッケージ化されていない拡張機能を読み込む」をクリックし、このプロジェクトのディレクトリ `apps/local-markdown-viewer` を選択します。
5.  インストールされた拡張機能の「詳細」をクリックします。
6.  **「ファイルの URL へのアクセスを許可する」をオンにします**（重要）。

## 使い方

- `.md` ファイルをChromeにドラッグ＆ドロップするか、直接ファイルパスを開くと自動的に変換されます。
- 画面右側の目次（または右下の目次ボタン）から各見出しへジャンプできます。

## 開発スタック

- JavaScript (Content Script)
- Vanilla CSS (GitHub Markdown Style)
- [Marked.js](https://marked.js.org/) (Markdown Parser)
